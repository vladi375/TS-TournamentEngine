import CheckboxFilter from './CheckboxFilter';

const PlayerRatingFiltersPane = () => {
  // filter for country
  // filter for end game type
  // filter for end game turn
  return <></>;
};

export default PlayerRatingFiltersPane;
